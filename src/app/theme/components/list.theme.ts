import type { MantineThemeComponents } from '@mantine/core';

export const listTheme: MantineThemeComponents['List'] = {
  defaultProps: {
    size: 'sm',
    spacing: 'xs',
    withPadding: true,
  },
};
