import type { MantineThemeComponents } from '@mantine/core';

export const buttonGroupTheme: MantineThemeComponents['ButtonGroup'] = {
  styles: {
    root: {
      gap: 'var(--mantine-spacing-xs)',
    },
  },
};
