import type { MantineThemeComponents } from '@mantine/core';

export const titleTheme: MantineThemeComponents['Title'] = {
  styles: {
    root: {
      color: 'var(--sl-text)',
      textWrap: 'balance',
    },
  },
};
